import 'package:flutter/material.dart';

class WriteBlog extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(style: TextStyle(color: Colors.black), "writing screen"),
      ),
    );
  }
}
